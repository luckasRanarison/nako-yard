関数（式の）RPNエバリュエータとは
    トークンリストは式を" "で区切る。
    スタックは[]。

    トークンリストを反復して
        もし(FLOAT(対象)がNAN判定)ならば
            Aはスタックから配列ポップのFLOAT。
            Bはスタックから配列ポップのFLOAT。

            対象で条件分岐
                "+"ならば、結果はBにAを足して。ここまで。
                "-"ならば、結果はBからAを引いて。ここまで。
                "*"ならば、結果はBにAを掛けて。ここまで。
                "/"ならば、結果はBをAで割って。ここまで。
            ここまで。

            結果をスタックに配列追加。
        違えば
            スタックに対象を配列追加。
        ここまで。
    ここまで。

    スタック＠0を戻す
ここまで。