関数（式の）トークン化とは
    トークンリストは[]。
    現在トークンは「」。
    式は式の" "を空に置換して。

    式を反復して
        もし、(FLOAT(対象)がNAN判定)でなければ
            現在トークンは現在トークンに対象を足して。
        違えば
            もし、現在トークンならばトークンリストに現在トークンを配列追加。
            現在トークンは「」。
            トークンリストに対象を配列追加。
        ここまで。
    ここまで。

    もし、現在トークンならばトークンリストに現在トークンを配列追加。

    トークンリストを戻す
ここまで。

関数（リストの）ピックとは
    リスト長さはリストの要素数。
    リスト[リスト長 - 1]を戻す
ここまで。